# Copy this file to .env.local and fill in your values
# This file is safe to commit — .env.local is NOT

# ─── WhatsApp ────────────────────────────────────────────
# Your WhatsApp number with country code, NO + sign
# Example: 919998887777 for India +91 999 888 7777
NEXT_PUBLIC_WHATSAPP_NUMBER=919999999999

# ─── Contentful CMS (optional) ───────────────────────────
# If left empty, the site uses the built-in product catalog from data/products.js
# Get these from: https://app.contentful.com → Space Settings → API Keys
NEXT_PUBLIC_CONTENTFUL_SPACE_ID=
NEXT_PUBLIC_CONTENTFUL_ACCESS_TOKEN=

# ─── Contentful Content Model Setup ──────────────────────
# Create a content type called "product" with these fields:
#   name        → Short Text
#   code        → Short Text  (e.g. WD1, WD2...)
#   description → Short Text
#   items       → Short Text, List
#   price       → Number
#   category    → Short Text  (womensday | spa | wellness)
#   featured    → Boolean
#   image       → Media (single file)
